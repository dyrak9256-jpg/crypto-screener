# Автоподключение источника данных Prometheus (uid используется в дашбордах).
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prom-screener
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
